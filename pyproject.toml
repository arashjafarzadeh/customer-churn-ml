[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-churn-ml"
version = "0.1.0"
description = "End-to-end customer churn prediction and actionable customer ranking project"
requires-python = ">=3.14"